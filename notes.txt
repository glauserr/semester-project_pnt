

SETUP:

	1. install docker and docker-compose
	2. checkout lightning repo lightningnetwork/lnd

CONFIG:
	Set permission to docker engine:
		$ sudo usermod -a -G docker $USER




	Docker for lnd:
	https://github.com/lightningnetwork/lnd/tree/master/docker